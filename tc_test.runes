f sum(a: i32, b: i32) -> i32 {
    return a + b
}

f main() {
    i32 x = 5
    i32 y = 10
    
    i32 result = sum(x, y)
    
    bool is_big = result > 100
    if is_big {
        // do nothing
    }
    
    // Deliberate type error for testing:
    i32 error_test = x + is_big
}
