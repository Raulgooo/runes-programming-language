-- EXPECT FAIL: must use self or self: *Value as its first parameter
interface Value {
    f get(value: i32) = result: i32
}

type Item = { value: i32 }

method Value for Item {
    f get(self) = result: i32 { result = self.value }
}
