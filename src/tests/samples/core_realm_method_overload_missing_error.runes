-- EXPECT FAIL: method 'value' has no regional definition

type Box = {
    item: i32
}

in dynamic method Box {
    f value(self) = result: i32 {
        result = self.item
    }
}

in gc method Box {
    f value(self) = result: i32 {
        result = self.item
    }
}

regional f read(box: Box) = result: i32 {
    result = box.value()
}

f main() {
    Box box = Box(item: 1)
    print(read(box))
}
